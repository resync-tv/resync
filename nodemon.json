{
  "watch": ["backend", "types"],
  "ext": "ts, json",
  "exec": "npx ts-node -r dotenv/config ./backend/index.ts",
  "events": {
    "start": "node -e console.clear()"
  },
  "env": {
    "NODE_ENV": "development",
    "DEBUG": "resync*"
  }
}
